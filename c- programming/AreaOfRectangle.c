//Area of the rectangle
#include<stdio.h>
int main()
{
   int l,b,area=l*b;
   printf("Enter the length of the of rectangle = ");
   scanf("%d",&l);
   printf("Enter the breadth of the rectangle = ");
   scanf("%d",&b);
   area=l*b;
   printf("Area of the rectangle = %d",area);
   return 0;
}